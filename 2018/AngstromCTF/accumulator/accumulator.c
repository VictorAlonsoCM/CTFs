#include <stdlib.h>
#include <stdio.h>

int main(){

	int accumulator = 0;
	int n;
	while (accumulator >= 0){
		accumulator++;
	}
	accumulator--;
	printf("The accumulator has a value of %d. You win!\n", accumulator);

}

